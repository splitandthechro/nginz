func hello () {
    print ("Hello from Iodine!");
    arr = [1, 2, 3];
    target = nginz.BufferTarget ("ArrayBuffer");
    usagehint = nginz.BufferUsageHint ("StaticDraw");
    buf = nginz.GLBuffer (target, arr, usagehint);
}